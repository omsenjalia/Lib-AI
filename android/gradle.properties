org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# fllama builds llama.cpp through a native assets build hook (hook/build.dart).
# That compiles CMake in Release, so Gradle needs to leave it enough heap and
# must not try to run its own NDK toolchain over the same sources.
android.enableJetifier=false
