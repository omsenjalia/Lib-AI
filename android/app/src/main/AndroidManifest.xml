<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      Library AI is offline-first. INTERNET is used for exactly two things:
      downloading a model the user explicitly asked for, and polling the
      HuggingFace API for version changes. Nothing else in the app touches the
      network, and every one of those paths is skipped silently when offline.

      ACCESS_NETWORK_STATE backs connectivity_plus, which is what enforces the
      hard Wi-Fi-only block on the 27B download.

      CAMERA is deliberately NOT declared. image_picker captures through the
      system camera app via ACTION_IMAGE_CAPTURE, which needs no permission -
      and declaring CAMERA without requesting it at runtime would break the
      capture intent instead of enabling it.

      No broad storage permission is declared. Optional model storage is
      user-granted through ACTION_OPEN_DOCUMENT_TREE and a persisted SAF URI;
      exports remain app-scoped and are shared through the system share sheet.

      POST_NOTIFICATIONS backs the download notification. Android 13+ requires
      it, it is requested only when the user starts a download, and a refusal
      does not cancel the transfer.

      The foreground-service permissions below are limited to the
      user-initiated dataSync transfer: Play policy treats a large model download
      requested by the user as a user-initiated data transfer. WAKE_LOCK and
      ACCESS_WIFI_STATE are needed only while that service owns its transfer
      locks; no background polling or unrelated foreground work is allowed.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- The S25 has 8-12 GB of RAM; tell the OS this app genuinely needs a
         large heap so the LMK is less eager to kill us mid-generation. -->
    <application
        android:label="Library AI"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:largeHeap="true"
        android:allowBackup="false"
        android:usesCleartextTraffic="false">
        <!-- A dataSync FGS exists only while a user-started GGUF transfer is
             active. This is the Play-policy user-initiated data-transfer case. -->
        <service
            android:name=".ModelDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Required by flutter_local_notifications to deliver notification
             action taps (the Cancel button on a download). Without it the
             button renders but does nothing. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver"
            android:exported="false" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- image_picker resolves the system camera app through this intent.
             Required for OCR mode on Android 11+, where package visibility is
             restricted by default. -->
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE"/>
        </intent>
    </queries>
</manifest>
