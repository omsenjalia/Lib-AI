package com.libraryai.library_ai

import io.flutter.embedding.android.FlutterActivity

/**
 * No plugin registration is needed here: fllama is an FFI plugin built through
 * Flutter's native assets pipeline (hook/build.dart), which links libfllama.so
 * into the app directly.
 */
class MainActivity : FlutterActivity()
